Programa que compara una lista de nombres de secuencias asignadas ("nombres.txt") con las entradas de la tercer columna de cada renglon de un archivo csv ("epicov.csv"). 
Si encuentra una coincidencia escribe todo el renglón donde se encontró la coincidencia en un nuevo archivo csv ("cur.csv")
